# PRD — AI PM 인테이크 에이전트 "PM Jude"

## 1. 문서 정보
- **제품명**: **PM Jude** *(v1.3에서 확정. 유래: Product Manager의 약어 PM이 Paul McCartney의 이니셜과 같다는 데서 착안한 "Hey Jude" 언어유희. 이전 임시 코드명 Gatekeeper를 대체하며, 문서 내 역할 서술의 "게이트키퍼"는 보통명사로 유지)*
- **버전**: v1.4 (확정 / Approved for build)
- **상태**: Phase 0(PoC + 소급 베이스라인) 착수 가능
- **작성일**: 2026-07-23 (최초) / **개정일**: 2026-07-25
- **변경 이력**:
  - **v1.4** — 2차 비판적 리뷰를 반영한 개정판. ① **완결성 스키마의 근거 확립**: 필수 슬롯을 일반 요구공학 문헌이 아니라 **이 조직의 실측 재질문 유형 분류표**에서 도출하도록 절차화(F2e 신설). 그 귀결로 **베이스라인 측정을 전향적 1~2주 기록에서 소급 아카이브 분석으로 전환**(§9) — 표본 부족·PoC 오염·2주 대기를 동시에 해소하고, 한 번의 분석으로 §10의 분모와 F2c의 슬롯 목록을 함께 산출한다. ② **상태 머신의 미정의 경로 3개 봉합**: (a) 요청자가 원리적으로 답할 수 없는 슬롯의 **`승격` 상태**와 조건부 게이트 상정 경로 신설(v1.3에 존재하던 데드락 해소), (b) 게이트 **라우팅·SLA·미응답 에스컬레이션** 명시(F5), (c) **중복 병합 종결 상태** 신설(F8)과 그 귀결로 **다중 요청자 최소 모델을 Phase 1로 승격**. ③ **계측 재설계**: 우회율을 자발적 태깅에서 **세션 귀속(provenance) 기반 자동 집계**로 교체하고, 롤아웃 정책 발효 후에는 채택 지표가 아니라 컴플라이언스 지표임을 명시. **선행 지표**(중도 이탈율·재사용률·요청자 소요 시간)를 §10으로 승격. ④ **핵심 원칙 7 "문서 단일 진실 원천" 신설** — 목업 어노테이션의 **역주입**(F4)과 **번역 무결성**(F3) 요구사항이 여기서 파생. ⑤ **조직 기억**: F2a 컨텍스트 검색 대상에 종결 세션·게이트 결정 이력(거절 사유) 추가. ⑥ **자체 원칙의 미적용 시정**: 배포 게이트를 프로세스가 아닌 **코드 강제**로(F12), 볼륨 적합성 렌즈를 **비용 가드레일·프로바이더 선택**에도 적용(§6). ⑦ **중단(kill/pivot) 기준 신설**(§9), 해자 논의를 **build-vs-buy 재결정 트리거**로 재프레이밍(§2.5·§11), Linear Agent 공개 베타(2026-03) 현황 반영.
  - v1.3 — 용어 정정(고정 오케스트레이션), 제품 성격 확정(내부 도구·글로벌 원격), 원칙 6 신설, F2 확장(컨텍스트 그라운딩·2층 판정·다국어), F4 조건 실행, F12 도구 채택 전환, F13 볼륨 조건부 자동화, 마일스톤 재조정(Slack 우선), 성공 지표 재정의.
  - v1.2 — Phase 0(PoC) 신설: `claude -p` 하네스(F14). Linear Enterprise 전제 반영.
  - v1.1 — 자기개선 원칙·F11~F13·평가 서브시스템 추가.
  - v1.0(2026-07-23) — 최초 확정.
- **대상 독자**: 제품/엔지니어링 리더, 풀스택 개발팀, 디자이너, 보안 담당, 비개발자 스테이크홀더(참조)
- **문서 성격**: 이 PRD만 읽으면 개발 착수가 가능한 완결형 요구사항 문서. "무엇을(what)/왜(why)"만 규정하며, "어떻게(how)"의 상세는 시스템 구성의 논리 경계 안에서 개발팀 재량에 맡긴다.

---

## 2. 제품 개요

### 2.1 배경·문제 정의
PM 직군이 없는 개발팀에서는 비개발자 스테이크홀더(영업, 마케팅, CS, 경영진 등)의 요청이 개발자에게 직접 꽂힌다. 이 요청은 대개 모호하고("대시보드 만들어줘"), 완결성(무엇을/왜/수용기준)이 결여되어 있다. 그 결과 개발자는 요구사항 해석·재질문·맥락 복원에 시간을 소모하고, 잘못 이해한 채 구현했다가 재작업하는 비용이 발생한다.

명확화 루프의 효과는 학술적으로 입증되어 있다. ClarifyGPT(Mu et al., FSE 2024, arXiv:2310.10996)는 요구사항의 모호성을 탐지해 표적 질문으로 해소하면 LLM 산출물 품질이 유의미하게 향상됨을 보였다(MBPP-sanitized 기준 GPT-4 Pass@1 70.96%→80.80%). 기존 도구는 이 간극을 메우지 못한다: ChatPRD는 PM 역량을 전제한 작성/코칭 도구이고, Linear Asks는 명확화 루프 없는 변환·라우팅이다.

**전제의 한계와 검증 계획** *(v1.4 신설)* — 위 문제 정의는 "재질문의 상당 부분이 요청자와의 대화로 해소 가능하다"를 암묵 전제한다. 그러나 실제 재질문의 일부는 요청자가 원리적으로 답할 수 없는 것(어느 테이블이 진실 원천인가, 기존 권한 모델을 따르는가, 과거 데이터는 어떻게 하는가)이다. 이 비율은 **Phase 0의 소급 아카이브 분석으로 실측하며**(§9), 결과에 따라 제품 설계를 조정한다 — 요청자 해소 불가 유형이 다수라면 명확화의 무게중심은 요청자 대화(F2b)가 아니라 컨텍스트 검색(F2a)과 오픈이슈 승격(F2c)으로 이동한다.

### 2.2 제품 성격
- **내부 엔터프라이즈 도구**다. 외부 판매·멀티테넌시·과금은 목적이 아니다. 다만 **기능은 리치해도 좋다** — 내부 도구라는 이유로 기능을 깎지 않되, 판매용 제품에만 필요한 것(과금, 마케팅 표면, 임의 조직 온보딩)은 만들지 않는다.
- **글로벌 원격 협업이 전제**다. 스테이크홀더와 개발자가 언어·시차를 달리한다. 따라서 (a) 명확화 질문의 **다국어 대응**(요청자의 언어로 질문), (b) **비동기 1급 설계**(세션이 채널과 시간대를 넘어 이어지고, 승인 게이트가 비동기로 동작하며, 알림이 수신자 시간대를 인지)가 기능 요구사항이다.
- **대체 가능성을 전제로 만든다** *(v1.4 신설)*. 내부 도구에 해자는 목적이 아니다. 트래커 벤더가 동등 기능을 제공하면 자체 유지 여부를 재결정하는 것이 정상이며(§2.5·§11), 어댑터 격리와 얇은 추상화는 **그 재결정을 싸게 만들기 위한 장치**다.

### 2.3 제품 비전
**모든 채널·언어·시간대에서 들어오는 모호한 요청을 방어(인테이크)하고, 개발자가 곧바로 구현에 착수할 수 있는 완결된 요구사항으로 정제하는, 채널에 의존하지 않으며 스스로 좋아지는 AI PM 인테이크 레이어.** 정제 품질은 다운스트림 실측 신호로 측정되어 프롬프트·스키마·하네스 개선으로 되먹임된다.

### 2.4 타겟 사용자 (페르소나)
- **스테이크홀더(요청자)** — 비개발자, 다국적·다시간대. *니즈*: 최소 마찰, 모국어/업무 언어로 대화, 시각적 확인(목업), 침묵 없는 회신, 자기 시간대에 맞는 알림, **답할 수 없는 질문 앞에서 요청이 멈추지 않을 것**.
- **대리 요청자** *(v1.4 신설)* — CS 리드가 고객 요구를, 팀 리드가 팀원 요구를 대신 전달하는 경우. 요청자와 실사용자가 다르다. *니즈*: 자신이 답할 수 있는 것과 원 요청자에게 물어야 하는 것의 구분, 원 요청자 확인이 필요한 항목의 명시. *설계 함의*: F2b의 "누가 쓰나" 질문만으로 부족하며, 세션은 **요청자 ≠ 실사용자**를 표현할 수 있어야 한다.
- **개발자(수신자)** — PM 없는 팀의 엔지니어. *니즈*: 완결된 스펙만 수신, 승인 게이트로 통제권 유지, Linear 안에서 작업, 목업이 구현 기준으로 오해되지 않기, 비동기 승인, **게이트가 새로운 부담이 되지 않을 것**(라우팅·SLA·자동 처리).
- **관리자/운영자** — 팀 리드/워크스페이스 admin. *니즈*: 권한·채널·비용 설정, 파이프라인 건강도 파악, 정제 품질 판독과 개선 사이클 운영, 거절 이의 제기의 최종 판단.

### 2.5 포지셔닝

| 항목 | ChatPRD | Linear Asks + Linear Agent(2026-03 공개 베타) | **PM Jude** |
|---|---|---|---|
| 주 사용자 | PM 역량 보유자 | 요청자 전원 | 비개발자 스테이크홀더 + PM 없는 개발팀 |
| 명확화 루프 | 없음(사용자 주도) | 트리아지·중복 탐지·스펙 초안은 있으나 **요청자와의 다회차 명확화 루프는 아님** | **있음(컨텍스트 그라운딩 + 표적 질문 + 2층 완결성 판정)** |
| 시각 검증 | 없음 | 없음 | **인터랙티브 목업(UI 요청에 한해)** |
| 개발자 통제 | 없음 | 트리아지 | **명시적 승인 게이트(라우팅·SLA 포함)** |
| 품질 개선 루프 | 없음 | 없음 | **다운스트림 신호 기반 닫힌 루프** |
| 코딩 관여 | 없음 | 코딩 에이전트 연동 | **프로덕션 코드 없음(의도적 배제)** |
| 채널 | 웹앱 중심 | Slack/Teams/이메일/웹폼 | **채널 비의존 코어 + Slack first-class** |

**차별점과 그 수명** *(v1.4 재작성)*: 현재의 차별점은 변환 *이전*의 다회차 명확화, 2층 완결성 게이트, 시각 검증 목업, 다운스트림 신호 기반 자기개선 루프다. 그러나 이 제품은 내부 도구이므로 **해자를 지키는 것이 목표가 아니다**. 2026-03 Linear Agent 공개 베타로 트리아지·중복 탐지·스펙 초안이 기본 기능에 편입되었고 채팅 표면이 Slack/Teams까지 확장되었다. 따라서 §11의 대응은 "해자 방어"가 아니라 **build-vs-buy 재결정 트리거**다:

> **재결정 조건** — 트래커 벤더가 (1) 요청자와의 다회차 명확화 루프와 (2) 완결성 게이트를 GA로 제공하면, F2·F3의 자체 유지 여부를 재결정한다. 판단 기준은 "우리 조직의 재질문 유형 분류표(F2e)를 벤더 기능이 얼마나 커버하는가"이며, 남는 차이가 목업·개선 루프뿐이라면 그 둘만 남기고 나머지는 폐기한다. 재평가 주기: 분기.

---

## 3. 핵심 원칙 (문서 전반에 일관 적용)

1. **채널 비의존(Channel-Agnostic)** — 인테이크·명확화·목업·승인·추적의 모든 코어 기능은 채널 무관 코어 API로 존재하며, 자체 웹 표면만으로 완결적으로 동작할 수 있어야 한다. Slack은 first-class 채널이지만 의존성이 아니다. 세션은 채널·시간대를 넘어 연속된다. **주의: 이것은 아키텍처 원칙이지 전달 순서가 아니다.** 전달 순서는 채택 리스크를 먼저 검증하도록 별도로 정한다(§9 — Slack 우선 전달).

2. **고정 오케스트레이션(Predefined Control Flow)** — 단계 전이(인테이크→명확화→문서→목업→게이트→이슈→추적)는 **코드로 작성된 상태 머신**이 결정하며, LLM은 단계 *안의* 구조화된 호출로만 존재한다. LLM에게 자율 루프가 허용되는 곳은 단 하나 — 명확화 전 컨텍스트 검색(F2a)의 경계 지어진 툴콜 루프뿐이다. 하드 제약("승인 없이 이슈 생성 불가", "코딩 비관여", "회귀 미통과 프롬프트 버전 로드 불가")은 프롬프트로 부탁하지 않고 **코드로 강제**한다. **용어 정밀성**: 이 시스템 전체는 결정론적이지 *않다* — LLM 출력은 확률적이다. 결정론이 성립하는 부분은 룰 기반 구조 검사(F2c)와 게이트 강제 코드뿐이며, 본 문서에서 "결정론적"은 그 두 곳에만 사용한다. 시스템의 정확한 기술은 "확률적 구성요소를 감싼 고정 오케스트레이션"이다.

3. **코딩 비관여(No-Production-Code)** — 에이전트는 프로덕션 개발에 관여하지 않는다. 산출물은 "무엇을/왜"(requirements)와 태스크 목록까지이며 "어떻게"(아키텍처·스택·코드)는 개발자와 코딩 에이전트의 몫이다. **솔직한 경계 명시**: 목업 생성은 기술적으로는 코드(HTML) 생성이다. 이 경계는 기술이 아니라 **정책과 산출물 취급 규율**로 강제된다 — 목업 코드는 저장소·이슈 어디에도 구현 기준으로 전달되지 않고, 이미지가 아닌 코드 형태로는 개발팀에 노출되지 않으며, "구현 기준 아님" 라벨이 상시 부착된다.

4. **모델 중립(Model-Neutral)** — 프로바이더 추상화 레이어를 1일차부터 도입한다. 모델 교체가 설정 한 곳 변경으로 가능해야 한다. **단, 얇게** *(v1.4)*: 추상화 구현체는 벤치마크 없이 지금 하나를 확정하고, 모델 라우팅 로직은 Phase 1 범위에서 제외한다(§6 — 볼륨 적합화). **PoC 예외**: Phase 0에 한해 Claude Code 헤드리스 하네스(F14)를 백엔드로 사용하되 게이트웨이 인터페이스 뒤에 격리한다.

5. **침묵 방지(No-Silence)** — 모든 요청은 반드시 상태 회신을 받는다. 승인/질문/병합/백로그/거절/보류 어느 경우든 사유를 담은 정중한 회신이 요청자의 언어와 시간대에 맞게 간다. **양방향 적용** *(v1.4)*: 이 원칙은 요청자뿐 아니라 게이트에도 적용된다 — 승인 대기 항목이 무기한 방치되는 경로는 존재하지 않으며, SLA 초과 시 자동 처리된다(F5).

6. **자기개선(Closed-Loop Improvement)** — 정제 품질의 궁극 판정자는 내부 점수가 아니라 **다운스트림 실측 신호**(개발자 반려·재질문, 이슈 reopen·스펙 변경, 목업 반복, 후행 피드백)다. 모든 세션은 프롬프트/모델/임계치 버전에 귀속되어 계측되고, 개선은 회귀 평가 통과 후에만 배포된다. **개선 대상은 프롬프트만이 아니다** *(v1.4)*: 재질문이 반복되는데 대응 슬롯이 없다면 그것은 프롬프트 결함이 아니라 **스키마 결손**이며, 개선 후보는 룰 목록 보강이다(F2e). **볼륨에 맞는 수단**: 낮은 요청량에서는 **운영자의 전수 인간 리뷰가 1차 평가 수단**이며, LLM-judge·A/B는 볼륨 임계치를 충족할 때만 도입한다. 계측(신호 스키마·버전 귀속)은 Phase 1부터 내장한다 — 계측 없는 기능은 만들지 않는다.

7. **문서 단일 진실 원천(Single Source of Truth)** *(v1.4 신설)* — `requirements` 문서만이 구현의 근거다. 대화 전사, 목업, 번역본, 채널 스레드 중 **어느 것도 문서를 대체하지 않는다**. 따라서 다른 표면에서 확정된 내용은 반드시 문서로 **역주입**된다:
   - 목업 어노테이션에서 합의된 사항은 승인 시 `requirements` vN+1에 문장으로 흡수되고, 흡수 후 목업은 폐기 가능해야 한다(F4). 목업에만 존재하고 문서에 없는 확정 사항이 남으면 안 된다.
   - 요청자 확인은 산문 번역본이 아니라 **문서의 구조화 슬롯 값**에 대해 요청자 언어로 수행하고, **원문 전사는 옵션이 아니라 상시 보존**한다(F3). 번역은 확률적 구성요소가 수행하는 손실 있는 변환이며, 요청자도 개발자도 검증할 수 없는 구간을 남겨서는 안 된다.

---

## 4. 사용자 시나리오 / 유저 스토리와 수용 기준

### US-1 — 인테이크 (웹)
스테이크홀더로서, 웹 채팅창에 자유롭게 요청을 입력하고 싶다.
- **수용기준**: 간편 로그인(매직 링크/SSO) 후 입력 가능 / 접수 즉시 확인 + 요청 ID 발급 / 코어 세션 생성 / 대시보드에 "명확화 중" 표시.

### US-2 — 인테이크 (Slack)
스테이크홀더로서, Slack에서 봇 멘션/DM/이모지로 요청을 넣고 싶다.
- **수용기준**: 멘션/DM 시 접수 확인이 스레드에 게시 / 동일 코어 세션 생성 / Linear 계정 불필요.

### US-3 — 명확화 (다국어)
스테이크홀더로서, **내 언어로 된** 비즈니스 질문에 답하며 요구를 구체화하고 싶다.
- **수용기준**: 질문은 요청자의 언어로, 한 번에 3~5개 / 질문 생성 전 기존 이슈·문서·**과거 결정 이력** 검색이 수행되어 이미 답이 있는 것은 묻지 않음 / 유사 기존 요청 발견 시 중복 후보 제시 / 완결성 임계치 도달 시 자동 종료 / 기술 용어 미사용.

### US-4 — 목업 확인 (UI 요청에 한해)
스테이크홀더로서, 내 요청이 어떤 화면으로 구현될지 눈으로 확인하고 싶다.
- **수용기준**: UI 변화를 수반하는 요청에만 목업 단계 실행 / 조작 가능한 호스팅 URL 전달 / "요구사항 확인용 목업 — 구현 결과 아님" 워터마크 상시 / 최대 2~3회 반복 수렴, 미수렴 시 에스컬레이션 / 피드백은 어노테이션·코멘트로 / **승인 시 어노테이션에서 확정된 사항이 요구사항 문서에 흡수됨**.

### US-5 — 개발자 승인 게이트 (비동기)
개발자로서, 내 시간대에 맞춰 최종 스펙을 검토하고 승인/질문/백로그/거절을 결정하고 싶다.
- **수용기준**: 4버튼 제공, 비동기 처리 / **내게 온 이유(라우팅 근거)가 표시됨** / SLA와 남은 시간 표시, 리마인더 / **승인 시에만** Linear 이슈 생성 / 반려·질문 시 사유 태그 1클릭(F11 귀속) / 질문 선택 시 요청자 되물음 루프 / **조건부 상정(미해결 오픈이슈 포함) 건은 시각적으로 구분**.

### US-6 — 진행도 역보고
스테이크홀더로서, 내 요청이 어디까지 왔는지 알고 싶다.
- **수용기준**: Linear 상태 변경 시 알림 / Slack 연동 시 DM, 미연동 시 웹 알림/이메일 / 수신자 시간대 인지 발송 / **병합된 요청도 동일하게 역보고 수신**.

### US-7 — 채널 전환
스테이크홀더로서, Slack에서 시작한 명확화를 웹에서 이어가고 싶다.
- **수용기준**: 세션 컨텍스트 채널 간 보존 / 웹에서 전체 히스토리 열람·이어쓰기 / 재설명 불필요.

### US-8 — 거절/백로그 회신 (침묵 방지)
스테이크홀더로서, 내 요청이 진행되지 않더라도 이유를 알고 싶다.
- **수용기준**: 거절/백로그 시 **통제된 사유 목록**에서 선택된 사유를 담은 정중한 회신 / **이의 경로(팀 리드 재검토 요청) 안내** *(v1.4)* / 백로그 항목은 재부상 트리거(§12)에 따라 다시 검토됨을 안내 / 무응답 종결 없음.

### US-9 — 품질 개선 운영
관리자/운영자로서, 정제가 실제로 잘 되는지 실측 신호로 확인하고, 실패 패턴을 근거로 프롬프트·스키마를 안전하게 개선하고 싶다.
- **수용기준**: 프롬프트 버전별 반려율·재질문율·목업 반복 추이 열람 / 전 세션 판독 큐와 판독 기록 저장 / 실패 사례가 유형별 분류되어 세션 드릴다운 가능 / **재질문 유형과 필수 슬롯의 매핑 현황 열람(미매핑 = 스키마 결손 후보)** *(v1.4)* / 프롬프트·스키마 변경은 골든셋 회귀 통과 없이 배포 불가 / 개선 배포 후 재측정 리포트.

### US-10 — 답할 수 없는 질문 *(v1.4 신설)*
스테이크홀더로서, 내가 답할 수 없는 질문 때문에 요청이 멈추지 않기를 바란다.
- **수용기준**: "모르겠다 / 개발팀이 정할 문제다"를 1클릭으로 표시 가능 / 해당 항목이 담당자 지정 오픈이슈로 승격되어 문서에 실림 / 세션이 미완결 상태로 방치되지 않고 조건부로 게이트에 상정됨 / 승격 사실과 사유가 요청자에게 회신됨.

### US-11 — 중복 요청 병합 *(v1.4 신설)*
스테이크홀더로서, 이미 같은 요청이 있다면 새로 만들지 말고 거기에 붙여 달라.
- **수용기준**: 중복 후보가 요청자 언어로 요약 제시됨(무엇이 같은지 근거 포함) / 수락 시 새 이슈를 만들지 않고 기존 이슈에 연결 / 요청자가 해당 이슈의 구독자로 등록되어 진행 상황 수신 / 내 요청 원문과 맥락이 기존 이슈에 코멘트로 첨부되어 소실되지 않음 / 거부 시 정상 명확화 루프로 복귀.

---

## 5. 기능 요구사항 (F1–F14)

기능은 **코어(채널 무관)**, **채널 어댑터**, **평가·개선**, **PoC 하네스**로 구분한다.

### F1 — 인테이크 (코어 + 웹 + Slack 어댑터)
- **F1-Core**: 채널 무관 수신, 통합 프로필·세션 생성, 즉시 확인 응답. 프로필에 **선호 언어·시간대** 저장. 세션은 **요청자 ≠ 실사용자**(대리 요청)와 **N명의 요청자**를 표현할 수 있는 스키마를 갖는다 *(v1.4 — 중복 병합의 전제)*.
- **F1-Web**: 웹 채팅 인테이크, 매직 링크/SSO.
- **F1-Slack**: 멘션/DM/이모지 인테이크. Bolt for TypeScript + Assistant 스레드, 스레드 컨텍스트 스토어.
- **수용기준**: 두 채널 모두 접수 3초 이내 확인 / 동일 코어 세션 스키마 / 처리 상태 표시 / 요청자 목록이 1명 이상 N명까지 확장 가능.

### F2 — 명확화 엔진 (코어)

**(a) 컨텍스트 그라운딩** — 질문 생성 *전에* 기존 자료를 검색해 요청을 맥락화한다. 검색 대상 *(v1.4 확장)*:
  1. 기존 Linear 이슈, 2. 과거 `requirements` 문서, 3. 등록된 제품 문서, **4. 종결된 과거 세션과 게이트 결정 이력(거절·백로그 사유 포함)**.
  4번이 v1.4의 추가분이다. 데이터는 F5/F11이 이미 수집하고 있으므로 추가 비용이 거의 없고, **조직 기억**이 여기서 생긴다 — "이 요청은 3월에 X 사유로 거절되었습니다"를 명확화 시작 시점에 즉시 회신할 수 있다.
  이 검색은 시스템에서 유일하게 LLM에게 자율성이 허용되는 **경계 지어진 툴콜 루프**다(검색 도구만 노출, 호출 횟수 상한). 효과: 이미 답이 있는 것을 묻지 않고, generic 질문을 방지하며, **유사 기존 요청을 중복 후보로 제시**한다.

**(b) 표적 질문 생성** — ClarifyGPT식: 동일 요청의 그럴듯한 해석 후보를 다중 전개하고, 해석이 갈라지는 지점(누가 쓰나, 어떤 질문에 답하나, 데이터는 어디서, 얼마나 자주)을 표적 질문으로 전환. 한 번에 3~5문항, 요청자의 언어로, 비즈니스 어휘로, 예시 선택지 제공. **각 질문에는 "모르겠다 / 개발팀이 정할 문제다" 응답 경로가 항상 함께 제시된다** *(v1.4 — US-10)*.

**(c) 2층 완결성 판정** *(v1.4 — 슬롯 3상태화)*

- **슬롯의 3상태**: 각 필수 슬롯은 `충족` / `미충족` / **`승격(요청자 해소 불가)`** 중 하나다. 세 번째가 v1.4 신설이다. 요청자가 원리적으로 답할 수 없는 항목(어느 테이블이 진실 원천인가, 기존 권한 모델을 따르는가, 과거 데이터 처리 방침 등)은 **담당자가 지정된 오픈이슈로 승격**되어 문서의 `오픈이슈` 필드에 실린다. 승격은 요청자의 1클릭(US-10) 또는 LLM 층의 판정으로 트리거된다.
- **룰 층(결정론적 백스톱)**: 통과 조건은 **"모든 필수 슬롯이 `충족` 또는 `승격`"**이다. `미충족`이 하나라도 남으면 LLM 점수와 무관하게 미정제 — 예: "대시보드 만들어줘"는 전 슬롯 `미충족`이므로 즉시 미정제. 추가 형식 검사: 모든 스토리에 Given/When/Then 존재, 수용기준 내 금칙 모호어("적당히", "빨리", "개선" 등 사전 정의 목록) 부재, EARS 구문 충족.
- **왕복 상한 도달 시의 강제 판정** *(v1.4 신설 — v1.3의 데드락 해소)*: v1.3에서는 "왕복 상한 존재 + 룰 미통과 시 게이트 진입 불가 + 침묵 금지"가 동시에 성립해, 상한에 도달했는데 슬롯이 빈 요청은 **진행도 종결도 불가능**했다. v1.4의 처리:
  1. 남은 `미충족` 슬롯 각각에 대해 **승격 가능 여부를 판정**한다.
  2. 승격 가능하면 오픈이슈로 올리고 **"조건부"로 게이트에 상정**한다. 게이트 UI는 이 상태를 시각적으로 구분한다(US-5).
  3. 승격도 불가하면(요청 자체가 무엇을 원하는지 판별 불가) 게이트에 상정하지 않고, 사유를 담은 회신 후 **`보류(정보 부족)`로 종결**한다. 요청자는 언제든 재개할 수 있다.
  - **어느 경우에도 세션이 멈춘 채 방치되지 않는다**(원칙 5).
- **LLM 층(의미 판정)**: 슬롯이 채워진 뒤에도 남은 의미적 모호성을 해석 발산 재검사와 루브릭 점수로 판정. LLM이 정제를 수행하고, 룰은 LLM의 알려진 약점(과신에 의한 조기 통과)을 막는 안전핀이다.

**(d) 다국어** — 대화는 요청자 언어, 산출 문서는 팀 표준 언어. **원문 전사는 상시 보존**(옵션 아님, 원칙 7).

**(e) 슬롯 목록의 근거 관리** *(v1.4 신설)* — 필수 슬롯 목록은 일반 요구공학 문헌이 아니라 **이 조직의 실측 재질문 데이터**에서 도출한다.
  - 최초 목록: Phase 0의 소급 아카이브 분석(§9)이 산출하는 **재질문 유형 분류표**에서 파생한다.
  - 갱신: F11의 재질문·reopen 신호로 분기 점검한다.
  - **판정 규칙**: 어떤 재질문 유형이 반복 발생하는데 대응 슬롯이 없다면 그것은 프롬프트 문제가 아니라 **스키마 결손**이며, F13의 개선 후보에 "룰 목록 보강"으로 올린다. 슬롯이 다 찬 문서가 여전히 재질문을 유발하는 상황에서 프롬프트만 손보는 것이 이 시스템의 가장 그럴듯한 실패 모드이므로, 이 구분을 제도화한다.
  - **수용기준**: 각 필수 슬롯이 최소 1개의 실측 재질문 유형에 매핑되어 근거를 남김 / 근거 없는 슬롯은 추가하지 않음 / 미매핑 재질문 유형이 US-9 대시보드에 노출.

**F2 전체 수용기준**: 각 질문이 특정 모호성 또는 공백 슬롯에 매핑 / 검색 결과가 세션에 기록(무엇을 참조했는지 추적 가능) / 룰·LLM 판정과 슬롯 3상태가 각각 세션에 기록 / 전 과정이 F11 신호 스키마로 로깅 / 왕복 상한 존재(수치는 §12) / 상한 도달 시 위 3분기 중 하나로 반드시 귀결.

### F3 — 요구사항 문서 생성 (코어)
- `requirements`(문제 / 사용자 / 스코프 / 유저스토리+수용기준(Given-When-Then, EARS) / 데이터 소스 / **오픈이슈(승격 슬롯 + 담당자)**) + `tasks` 분해. "어떻게"는 비운다.
- **번역 무결성** *(v1.4 신설 — 원칙 7)*: 문서는 팀 표준 언어로 산출하되,
  - **원문 전사를 상시 보존**하고 이슈에 첨부한다(옵션 아님).
  - **요청자 확인은 산문 번역본이 아니라 구조화 슬롯 값 단위로, 요청자 언어로** 제시한다. 산문의 왕복 번역은 오류를 감추지만, 슬롯 단위 확인("대상 사용자: 영업팀 매니저 — 맞습니까?")은 오류를 드러낸다.
  - 요청자도 개발자도 검증할 수 없는 번역 구간을 남기지 않는다. 판독 큐(F13)는 표본 대조를 점검 항목으로 갖는다.
- **수용기준**: 아키텍처·코드·스택 미포함 / 각 스토리에 검증 가능한 EARS 수용기준 / 문서 vN이 세션·프롬프트 버전에 귀속 / 원문 전사 첨부 / 요청자 확인이 슬롯 단위·요청자 언어로 수행된 기록 존재.

### F4 — 목업 생성·호스팅·피드백 (코어, **조건 실행**)
- **전제 조건**: 명확화 종료 시 요청을 분류한다 — **UI 변화를 수반하는가?** 비 UI 요청(데이터 수정, 리포트/배치, 성능, 인프라 등)은 목업 단계를 **생략**하고 F3의 슬롯 단위 문서 검증으로 대체한다.
- UI 요청: 자체 LLM이 단일 self-contained **인터랙티브 HTML** 목업 생성(중간충실도, 그레이스케일, 더미데이터, "요구사항 확인용 목업 — 구현 결과 아님" 워터마크). 호스팅된 URL을 채팅에 전달.
- **호스팅·보안**: 앱 도메인과 분리된 샌드박스 usercontent 도메인(Cloudflare R2 + Workers 권장), 만료 서명 URL, CSP `sandbox` + `default-src 'none'` + 외부 네트워크 차단, 뷰어 iframe `sandbox="allow-scripts"`(allow-same-origin 배제), postMessage origin 검증, Slack 전송 시 `unfurl_links:false`/`unfurl_media:false`.
- **버전 매핑**: 요구사항 vN ↔ 목업 vN ↔ URL vN. 최대 2~3회 반복 후 수렴 또는 에스컬레이션.
- **역주입(문서 흡수)** *(v1.4 신설 — 원칙 7)*: 목업 승인 시, 반복 과정의 어노테이션·코멘트에서 **확정된 요구사항을 `requirements` vN+1의 문장으로 흡수**한다(예: "목업 v2에서 확정 — 필터 3종(기간·팀·상태), 기본 기간 30일, 빈 상태 문구 표시"). 2~3회 반복하면 목업에만 존재하고 문서에는 없는 확정 사항이 반드시 생기는데, F6은 목업을 "구현 기준 아님"으로 보내므로 그 정보는 공식적으로 아무 데도 없게 된다. 역주입은 이 누수를 막는다.
  - 흡수 후 **목업은 폐기 가능해야 한다** — 목업을 지워도 문서만으로 구현 가능한 상태가 목표다.
  - 게이트는 흡수 완료 여부를 표시하고, 판독 큐는 어노테이션 대비 문서 반영 누락을 점검 항목으로 갖는다.
- **수용기준**: 비 UI 요청에서 목업 미실행 / UI 요청에서 조작 가능 URL 전달 / 워터마크 상시 / 반복·어노테이션이 F11 신호로 기록 / **목업 반복 종료 시 어노테이션 대비 문서 반영 누락 0**.

### F5 — 개발자 승인 게이트 (코어)
- 승인/질문/백로그/거절 4버튼, **비동기 동작**. **승인 시에만** Linear 이슈 생성 — 이 제약은 상태 머신 코드로 강제(원칙 2).
- **라우팅** *(v1.4 신설)*: 게이트 수신자는 (1) 요청이 매핑된 Linear 팀/영역의 담당자, (2) 미매핑 시 팀 기본 승인자(F10 설정), (3) 부재(휴가·상태) 시 대체자 순으로 결정한다. **라우팅 근거를 세션에 기록하고 게이트 UI에 표시**한다("이 요청이 당신에게 온 이유: 대시보드 영역 담당"). PM 없는 팀에서 "이 요청은 누구 것인가"는 사소한 문제가 아니며, 이 제품은 모든 요청을 게이트로 모으므로 **새 병목을 하나 만든다**는 사실을 설계에 반영한다.
- **SLA와 미응답 처리** *(v1.4 신설)*: 게이트 항목은 SLA(F10 설정, 수신자 근무시간 기준)를 갖는다. 1차 리마인더 → 2차 리마인더 + 대체자 병행 통보 → **SLA 초과 시 자동 백로그 전환 + 요청자 회신**. 게이트가 무기한 대기 상태로 남는 경로는 존재하지 않는다(원칙 5의 양방향 적용).
- **거절 사유와 이의 경로** *(v1.4 신설)*: 거절은 **통제된 사유 목록**(우선순위 / 스코프 밖 / 중복 / 기술적 불가 / 정보 부족)에서 선택한다. 회신에는 **이의 경로(팀 리드 재검토 요청)**를 명시한다. 개발자가 조직 서열이 높은 요청자를 거절하는 상황은 실재하며, 이의 경로가 제품 안에 없으면 그 갈등은 제품 밖의 우회로 해소된다.
- **조건부 상정** *(v1.4)*: 오픈이슈로 승격된 슬롯이 있는 건은 시각적으로 구분 표시되고, 게이트 결정 시 오픈이슈의 담당자 지정이 함께 확정된다.
- **수용기준**: 승인 없이 이슈 미생성 / 모든 결정과 라우팅 근거 기록 / 반려·질문 시 사유 태그 1클릭(F11 귀속) / SLA 초과 건이 0으로 수렴(대기 상태 잔존 없음) / 질문·백로그·거절·보류 시 F8 회신 트리거.

### F6 — Linear 연동: 이슈 생성 (코어 커넥터)
- GraphQL `issueCreate`(@linear/sdk). 본문에 스펙 + **원문 전사 첨부** + 목업 링크("참고용, 구현 기준 아님" 라벨) + 오픈이슈(담당자 지정) 섹션.
- **세션 귀속(provenance)** *(v1.4 신설)*: 생성된 모든 이슈에 세션 ID를 **기계 판독 가능한 형태**(커스텀 필드 또는 규약된 라벨)로 남긴다. 이것이 §10 우회율 자동 집계의 전제다 — 귀속 없는 기능 요청 이슈 = 우회 후보.
- **중복 병합 처리** *(v1.4)*: 병합 세션은 새 이슈를 만들지 않고 기존 이슈에 요청자를 구독자로 추가하고 요청 맥락을 코멘트로 첨부한다.
- **수용기준**: 승인 즉시 생성 / 라벨링 / 실패 시 재시도·오류 알림 / 이슈 ID 세션 귀속 / **모든 생성 이슈에 세션 provenance 존재**.

### F7 — 진행도 추적·역보고 (코어 + 채널 알림)
- Linear 웹훅 기반(폴링 지양), Issue/Comment 구독, `Linear-Signature` HMAC-SHA256 + `webhookTimestamp` 1분 검증 + `webhookId` 중복 제거, 10초 이내 응답(비동기 처리).
- 알림: Slack 연동 시 DM, 미연동 시 웹/이메일. 수신자 시간대 인지. **요청자가 N명이면 각자의 언어·시간대로 팬아웃** *(v1.4)*.
- **"스펙 변경" 신호의 판정 규칙** *(v1.4 신설)*: 이슈 본문의 임의 편집을 신호로 삼지 않는다 — 서식·오탈자 수정이 섞여 노이즈가 된다. PM Jude가 생성한 **구조화 섹션(스코프 / 수용기준 / 데이터 소스)의 의미 변경**만 카운트하며, 판정은 **섹션 단위 diff → LLM 의미 변경 여부 확인**의 2단계로 수행한다. 판정 결과는 세션에 근거와 함께 기록한다.
- **수용기준**: 서명 실패 4xx / 상태 변경의 채널별 전달 / 이슈 라이프사이클 신호(재질문·reopen·스펙 변경·취소)가 위 규칙에 따라 F11로 추출·귀속.

### F8 — 종결 상태와 회신 UX (코어) *(v1.4 확장)*
- **종결 상태 목록** *(v1.4 갱신)*: `이슈 생성(승인)` / **`중복 병합`** / `백로그` / `거절` / **`보류(정보 부족)`**. 모든 종결 상태는 회신을 트리거한다.
- **중복 병합** *(v1.4 신설)*: F2a가 제시한 중복 후보를 요청자가 수락하면 새 이슈 없이 기존 이슈에 연결한다. 요청자는 구독자로 등록되어 **F7 역보고를 그대로 수신**하고, 요청 원문·맥락은 기존 이슈에 코멘트로 첨부되어 소실되지 않는다. 인테이크에서 ROI가 가장 높은 기능이므로 절반만 구현하지 않는다.
- **다중 요청자** *(v1.4 — Phase 1 필수로 승격)*: 병합이 동작하는 순간 1이슈 N요청자가 즉시 발생한다. 따라서 Phase 1은 **최소 모델**(요청자 목록 보유 + 역보고 팬아웃 + 각자 언어·시간대 적용)을 반드시 포함한다. 상충 해소 절차의 정식 모델은 §12.
- **거절/백로그**: 통제된 사유 목록 기반의 정중한 회신(요청자 언어) + **이의 경로 안내**. **백로그 재부상**: 유사 요청 누적(중복 후보 매칭 N회) 또는 정기 리뷰 주기 도래 시 게이트에 재상정하고 요청자에게 안내(트리거 수치는 §12).
- **수용기준**: 5개 종결 상태 모두가 회신 트리거 / 사유 필수 / 병합 시 구독 등록과 원문 첨부 확인 / 재부상 이벤트 기록.

### F9 — 요청 대시보드 (웹)
- 요청 목록/상태/진행도. 스테이크홀더/개발자/관리자 뷰. 개발자용 승인 대기 큐(SLA 잔여 표시), 관리자용 파이프라인·품질 지표.
- **v1.4 추가 뷰**: 판독 큐(전수 리뷰), **우회 후보 큐**(provenance 없는 기능 요청 이슈), **재질문 유형 ↔ 슬롯 매핑 현황**(미매핑 = 스키마 결손 후보).
- **수용기준**: 세션별 채널·상태·리드타임 표시 / 시간대별 표기 현지화 / 게이트 대기 시간이 리드타임과 분리 표시.

### F10 — 관리 설정 (웹)
- 승인 권한, **게이트 라우팅 규칙·SLA·근무시간 정의** *(v1.4)*, **거절 사유 목록 관리** *(v1.4)*, 채널 연결(Slack on/off), 비용 상한, 팀 표준 문서 언어, 백로그 재부상 규칙, 평가·개선 설정(리뷰 담당, 배포 승인자), **롤아웃 정책 텍스트 관리**("모든 기능 요청은 PM Jude 경유" 공지문), **우회 집계 필터 정의**(어떤 이슈 타입/라벨을 "기능 요청"으로 볼 것인가) *(v1.4)*.
- **수용기준**: admin 전용 / 비용 상한 가드레일 / 채널 해제 시 자동 폴백 / SLA·라우팅 변경이 즉시 반영.

### F11 — 품질 신호 수집·귀속 (평가·개선)
정제 품질의 궁극 판정자인 **다운스트림 실측 신호**를 수집하고, 각 신호를 세션과 당시의 프롬프트/모델/임계치/**슬롯 스키마** 버전에 귀속한다 *(v1.4 — 스키마 버전 추가)*.

| 신호 | 출처 | 의미 |
|---|---|---|
| 승인 게이트 결정 + 사유 태그 | F5 버튼 | 1차 품질 판정(반려/질문율 = 정제 실패의 선행 지표) |
| 게이트 대기 시간·SLA 초과 *(v1.4)* | F5 | 게이트 병목의 조기 경보 |
| 이슈 재질문 코멘트 | F7 웹훅 — 개발자가 "스펙 불충분" 1액션 표시 | 승인 통과 후 드러난 정제 결함. **유형 분류가 F2e 슬롯 갱신의 입력** |
| 이슈 reopen·스펙 변경·취소 | F7 웹훅(스펙 변경은 §F7 판정 규칙 적용) | 구현 단계에서 드러난 요구사항 오류 |
| 목업 반복 횟수·어노테이션 밀도 | F4 | 명확화가 시각화 전에 수렴 못 한 정도 |
| **목업 역주입 누락** *(v1.4)* | F4 판독 점검 | 원칙 7 위반(문서-목업 진실 원천 분기) |
| 스테이크홀더 후행 피드백("이게 아니에요") | 완료 역보고 1클릭 | 최종 결과-의도 불일치 |
| 완료 시 CSAT(1클릭, 양측) | 완료 역보고 | 만족도 |
| 명확화 왕복 수·완결성 점수·**슬롯 승격 건수** *(v1.4)* | F2 로그 | 대화 품질 / 요청자 해소 불가 비율(§2.1 전제 검증) |
| **세션 중도 이탈** *(v1.4 — 별도 분리)* | F2 로그 | **채택의 선행 지표.** 우회율보다 먼저 움직인다 |
| **재사용률** *(v1.4 신설)* | 요청자별 세션 이력 | 정책 강제 하에서 **자발적 채택의 유일한 신호** |
| **우회 감지 (provenance)** *(v1.4 — 측정 방식 교체)* | Linear 이슈 스캔: 세션 귀속 없이 생성된 기능 요청 이슈 | 정책 준수/채택 측정. **개발자 액션 0, 전수 집계.** 자발적 태깅은 보조 수단으로만 유지 |

- **수용기준**: 모든 신호가 `세션 ID × 프롬프트 버전 × 모델 버전 × 임계치 버전 × 슬롯 스키마 버전` 귀속 / 신호 스키마는 Phase 1부터 존재 / 개발자 표시가 Linear 안에서 1액션 / 수집이 사용자 흐름을 방해하지 않음(1클릭 이하) / **우회 집계가 개발자 행동에 의존하지 않음**.

### F12 — 평가 하네스: 구축이 아니라 채택
프롬프트·스키마·임계치 변경을 안전하게 만들기 위한 평가 기반. **범용 인프라(트레이싱, 프롬프트 버전 관리, 평가 실행, 대시보드)는 자체 구축하지 않고 기존 도구를 채택한다** — 후보: Langfuse(오픈소스, 셀프호스트), promptfoo(회귀 평가 러너). 자체 구축 범위는 도메인 특화 부분으로 한정: F11 신호 귀속 스키마, 골든셋 큐레이션 절차, 배포 게이트 연동.
- **골든 데이터셋**: 익명화된 실제 세션에서 큐레이션(PoC 세션이 시드). 성공·실패 사례 포함, 평가용/개선용 분리, 분기 갱신, 골든셋 자체도 버전 관리.
- **배포 게이트는 프로세스가 아니라 통제다** *(v1.4 시정)*: v1.3의 "회귀 미통과 변경의 배포 불가"는 프롬프트가 평가 도구 UI에서 편집 가능한 이상 **프로세스에 불과**했다. 원칙 2("하드 제약은 코드로 강제")를 여기에도 적용한다 — **런타임 게이트웨이는 `회귀 통과` 플래그가 붙은 프롬프트/스키마 버전만 로드**하며, 플래그 없는 버전은 도구 UI에서 저장되더라도 프로덕션 세션에 사용될 수 없다.
- **프롬프트 레지스트리**: 채택 도구의 버전 관리 기능 사용. 런타임 세션은 항상 명시적 버전 참조(F11 귀속의 전제).
- **LLM-as-judge는 기본 비활성**: 도입 조건(§12 임계치) 충족 전까지 회귀 평가의 채점은 룰 검사 + 운영자 판독으로 수행.
- **수용기준**: 플래그 없는 버전의 런타임 로드가 코드 수준에서 불가 / 평가 실행 재현 가능 / 채택 도구가 셀프호스트 또는 사내 데이터 정책 충족.

### F13 — 개선 루프 운영: 볼륨에 맞는 자동화
신호 → 진단 → 개선 → 검증 → 배포 → 재측정의 닫힌 사이클. **자동화 수준은 요청량에 따라 단계 도입한다.**

```
[F11 신호 수집]
      ↓
[전수 인간 리뷰] — 운영자가 모든 세션 판독(저볼륨 1차 수단)
   실패 유형 분류: 질문 미스타겟 / 완결성 과대평가 / 목업 오해 유발 /
   문서 구조 결함 / 언어 부적합 / 스코프 판단 오류 /
   [v1.4 추가] 스키마 결손(슬롯 없음) / 번역 무결성 위반 / 목업 역주입 누락
   ※ 판독 기록 = 미래 LLM-judge 보정용 라벨 자산
      ↓
[개선 후보 분기] — (a) 프롬프트 수정·few-shot 추가·임계치 조정
                  (b) [v1.4] 슬롯 스키마 보강 ← 재질문 유형에 대응 슬롯이 없을 때
      ↓
[인간 승인 게이트] — 운영자 검토·승인(자동 배포 금지)
      ↓
[오프라인 회귀 평가] — F12 골든셋 통과 필수(코드 강제 배포 게이트)
      ↓
[카나리 배포] — 신규 세션 일부에 새 버전(볼륨 무관 유지되는 안전장치)
      ↓
[재측정 리포트] — 버전별 F11 신호 비교 → 승격/롤백
      ↺
```

- **개선 후보 분기가 v1.4의 핵심 추가다.** 슬롯이 다 찬 "완결" 문서가 여전히 재질문을 유발하는데 프롬프트만 반복 수정하는 것이 이 시스템의 가장 그럴듯한 헛돌기다. 판독 시 **"이 재질문에 대응하는 슬롯이 있는가"**를 먼저 묻고, 없으면 프롬프트가 아니라 스키마를 고친다.
- **자동화 단계와 도입 조건**: **L1** 전수 인간 리뷰 + 수동 개선(Phase 2 기본) → **L2** LLM-judge 보조 채점 + 실패 자동 분류(도입 조건: 주간 세션 수·축적 라벨 수 임계치 충족, judge-인간 일치율 검증 통과) → **L3** A/B 실험·자동 개선 제안(도입 조건: 통계적 검정력이 확보되는 트래픽 — 내부 도구에서는 도달하지 않을 수 있으며, 도달하지 않으면 도입하지 않는다). 어느 단계에서도 인간 승인 게이트는 유지.
- **수용기준**: 전 세션이 판독 큐에 적재되고 판독 기록이 라벨로 저장 / 실패 유형이 프롬프트 결함과 스키마 결손으로 구분 분류 / 모든 개선 배포에 카나리와 재측정 리포트 / 승격·롤백 기록 / 자동 제안은 예외 없이 인간 승인 경유.

### F14 — PoC LLM 하네스: Claude Code 헤드리스 (*Phase 0 한정*)
PoC에서는 프로바이더 API 직접 구성 대신 **Claude Code 헤드리스 하네스**를 게이트웨이의 첫 백엔드로 사용한다 — CLI `claude -p` 또는 **Claude Agent SDK**(세션·구조화 출력·툴 권한 제어를 SDK가 처리하므로 권장). 목적: 인프라 최소화, **명확화·문서 생성 프롬프트 품질의 조기 검증**.
- **호출 규약**: 게이트웨이 표준 인터페이스(`complete(promptVersion, input) → structuredOutput`) 뒤에 격리. CLI 사용 시 `--output-format json` 파싱·스키마 검증. 프롬프트 본문은 반드시 레지스트리 버전 참조 — PoC 세션도 F11 귀속 대상.
- **안전 규약**: 파일시스템·셸 도구 비활성화(F2a 검색 도구만 예외적으로 허용 가능), 서버 측 격리 실행, 사용자 입력을 셸 인자로 보간하지 않음(stdin/임시 파일).
- **운영 규약**: 타임아웃·동시 실행 상한, 비용/사용량 로깅. 세션 컨텍스트는 자체 저장소에서 조립해 **무상태 호출**로 주입.
- **전환(Exit) 기준**: PoC 목표 달성 시 직접 API로 교체, 하네스는 폐기 또는 로컬 개발 전용 격하. 전환 전후 골든셋 시드로 출력 동등성 확인 + 타 프로바이더 교차 평가(단일 모델 고착 방지).
- **수용기준**: 하네스 호출이 게이트웨이 외부로 누출되지 않음 / PoC 세션도 버전 귀속 / 도구 제한 상태로만 실행 / 전환 시 코드 변경이 게이트웨이 구현체 1곳으로 한정.

---

## 6. 비기능 요구사항
- **보안 — 목업 호스팅 다층 방어**: ① 샌드박스 usercontent 도메인 분리 ② 만료 서명 URL(HMAC-SHA256) ③ CSP `sandbox` + `default-src 'none'` + 외부 네트워크 차단 ④ 뷰어 iframe `sandbox="allow-scripts"`(allow-same-origin 배제) + postMessage origin 검증 ⑤ Slack 전송 시 `unfurl_links:false`/`unfurl_media:false` ⑥ Linear 웹훅 서명·타임스탬프 검증.
- **다국어·시차**: 대화는 요청자 언어 / 문서는 팀 표준 언어 / **원문 전사 상시 보존** / 요청자 확인은 슬롯 단위·요청자 언어(원칙 7) / 알림·리마인더는 수신자 시간대 인지 / 게이트는 비동기 기본이되 SLA를 갖는다.
- **평가 데이터 프라이버시**: 골든셋·판독 기록 큐레이션 시 개인·사내 민감 정보 익명화, 보존 기한, 목적 제한. 평가 도구는 셀프호스트 또는 사내 데이터 정책 충족(F12).
- **모델 중립 — 얇게** *(v1.4 조정)*: 프로바이더 추상화는 **하나를 지금 확정**하고(벤치마크 없이; Vercel AI SDK / LiteLLM / Bedrock Converse 중 팀 스택에 맞는 것), 교체가 설정 한 곳 변경이면 충분하다. judge 도입 시 judge 모델은 생성 모델과 독립 교체 가능.
- **비용 가드레일 — 볼륨 적합화** *(v1.4 재조정)*: 내부 도구의 요청량(주 수십 건 규모)에서 세션당 LLM 비용은 자릿수 추정상 제약이 아니다. 따라서 **모델 선택 기준은 비용이 아니라 정제 품질**이며, Phase 1에서 **모델 라우팅 로직과 프로바이더 벤치마크는 범위 밖**이다. 남기는 것은 **폭주 방지 상한**뿐: 세션당 토큰 상한, 목업 생성 횟수 상한, 월 총액 알림. (v1.3이 평가 서브시스템에 적용한 "볼륨에 맞는 수단" 렌즈를 비용 영역에도 동일하게 적용한 것이다. 근거 없는 비용 논의를 문서에 남기지 않는다.)
- **세션·버전 관리**: 요구사항 vN ↔ 목업 vN ↔ URL vN + 세션 × 프롬프트/모델/임계치/**슬롯 스키마** 버전 귀속. 채널 간 세션 연속성.
- **성능/응답성**: 인테이크 확인 3초 이내 / 웹훅 10초 이내 응답 / 매직 링크 만료 15~30분.
- **관측성**: 명확화 왕복 수·완결성 점수·슬롯 승격률·목업 반복·**게이트 대기 시간**·리드타임 + 버전별 품질 신호·개선 사이클 리드타임·우회율·중도 이탈율·재사용률.

---

## 7. 시스템 구성 (고수준)
> "어떻게"의 상세는 개발팀 몫. 아래는 논리적 경계만 규정한다.

**오케스트레이션 명세** *(원칙 2의 구현 경계)*

상태 머신의 전이(v1.4 기준 전체 경로):

```
인테이크
  ↓
컨텍스트 검색(F2a) ──[중복 후보 수락]──→ [중복 병합] → 구독 등록 → 추적/역보고
  ↓
명확화 루프(F2b/c) ──[왕복 상한 도달]──→ 승격 판정
  ↓                                        ├─[승격 가능]→ 오픈이슈 승격 → 조건부 게이트 상정
  ↓                                        └─[승격 불가]→ [보류(정보 부족)] → 회신
문서 생성(F3) + 슬롯 단위 요청자 확인
  ↓
[UI 요청이면] 목업 반복(F4) → 승인 시 역주입 → 문서 vN+1
  ↓
승인 게이트(F5) ─[SLA 초과]→ 자동 [백로그] → 회신
  ├─[승인]→ 이슈 생성(F6, provenance 부착) → 추적/역보고(F7)
  ├─[질문]→ 명확화 루프로 복귀
  ├─[백로그]→ 회신 + 재부상 대기
  └─[거절]→ 사유 선택 + 이의 경로 안내 → 회신
```

- LLM은 단계 안의 **구조화된 호출 5종**으로만 존재: ① 표적 질문 생성 ② 완결성 의미 채점(슬롯 3상태 판정 포함) ③ requirements/tasks 생성 ④ 목업 HTML 생성 ⑤ (L2 이후) 품질 채점. 여기에 v1.4가 추가한 보조 호출: 목업 어노테이션의 역주입 문장화, 스펙 변경 의미 판정(F7).
- LLM 자율 툴콜이 허용되는 유일한 지점: F2a 컨텍스트 검색(검색 도구만 노출, 호출 상한).
- **하드 제약은 코드로 강제**: 승인 없는 이슈 생성 불가 / 룰 층 미통과 시 게이트 진입 불가(승격 경로 제외) / 목업 코드 비전달 / **회귀 미통과 프롬프트·스키마 버전 로드 불가**(F12) / **종결 상태 전이 시 회신 발송 없이는 세션 종료 불가**(원칙 5).

**채널 비의존 코어**: 인테이크 API / 명확화 엔진(검색기 + 질문 생성 + 2층 판정 + 승격 판정) / 컨텍스트 검색기(Linear·문서 인덱스·**종결 세션 인덱스**) / 문서 생성기 / 요청 분류기(UI 여부) / 목업 서비스(+ **역주입기**) / 게이트·워크플로 상태 머신(+ **라우터·SLA 스케줄러**) / Linear 커넥터 / 세션·버전 저장소 / 알림 서비스(언어·시간대 인지, N요청자 팬아웃, 채널 폴백).

**평가·개선 서브시스템**: 신호 수집기(F11) / **우회 스캐너**(provenance 없는 이슈 집계) / 채택된 평가 도구(트레이싱·프롬프트 레지스트리·회귀 실행) / 판독 워크벤치(전수 리뷰 큐·라벨 저장·개선안 승인 흐름·카나리 관리·**슬롯 매핑 현황**).

**채널 어댑터**: 웹(완결 표면) / Slack(멘션·DM·이모지 인테이크, 스레드 명확화, 버튼 게이트, DM 역보고, 웹 딥링크) / 어댑터 인터페이스 명시(향후 Teams/이메일). 모든 어댑터는 동일 코어 API만 호출.

**LLM 게이트웨이**: 얇은 프로바이더 추상화 + 폭주 방지 상한(라우팅 로직 없음). 백엔드: (Phase 0) Claude Code 헤드리스(F14) → (Phase 1+) 직접 API. 게이트웨이 외부는 백엔드 종류를 모른다.

---

## 8. 스코프 제외 (Non-goals)
- 프로덕션 코드 생성·아키텍처 설계·기술 스택 결정.
- 외부 판매·멀티테넌시·과금·임의 조직 온보딩(내부 도구 확정).
- 목업을 구현 결과물로 납품하거나 하이파이 디자인 제공.
- 완전 자율 이슈 종결(승인 게이트 필수) / 인간 승인 없는 자동 프롬프트·스키마 배포.
- Linear 외 트래커 1차 지원(Jira 등 향후).
- PM 코칭/전략 수립(ChatPRD 영역).
- **모델 라우팅·프로바이더 벤치마크** *(v1.4 추가 — 볼륨상 불필요, §6)*.
- **요청 우선순위 결정·상충 조정의 자동화** *(v1.4 명시)*. 이 제품은 요구를 **정제**하지 결정하지 않는다. 우선순위와 상충 판단은 게이트의 개발자·팀 리드 몫이며, 제품은 판단에 필요한 재료(중복 후보, 과거 결정 이력, 요청자 목록)를 제공하는 데까지다. — *이 경계를 명시하는 이유: "AI PM"이라는 이름이 결정 자동화를 기대하게 만들지만, 결정은 조직 권한의 문제이지 정제 품질의 문제가 아니다.*

---

## 9. 마일스톤 / 단계별 범위
> 원칙: 채널 비의존은 아키텍처로 지키되, **전달 순서는 가장 위험한 가정(스테이크홀더 채택)부터 검증**한다. 스테이크홀더의 서식지는 Slack이므로 PoC와 첫 전달 표면을 Slack에 둔다.
> *(v1.4)* 각 Phase는 통과 조건뿐 아니라 **중단·전환(kill/pivot) 기준**을 갖는다. 가설이 틀렸을 때의 분기가 없는 계획은 계획이 아니다.

### Phase 0 — PoC + 소급 베이스라인 (1~2주)
- **PoC를 Slack 스레드에서 실행** — 채택 가설과 프롬프트 품질(명확화·문서 생성)을 스테이크홀더의 서식지에서 동시 검증. 하네스: Claude Agent SDK 또는 `claude -p`(F14). Linear는 `issueCreate` 1개 또는 수동. 목업·웹훅·대시보드 범위 외.
- **베이스라인 — 소급 아카이브 분석** *(v1.4 전환)*: v1.3의 "도입 전 1~2주 전향적 기록"을 폐기한다. 대신 최근 6~12개월의 Linear 아카이브에서 개발자의 명확화성 코멘트·reopen·스펙 편집을 추출해 두 가지를 산출한다.
  - (a) **재질문 빈도 베이스라인** — §10 감소율 지표의 분모.
  - (b) **재질문 유형 분류표** — F2e가 필수 슬롯 목록을 도출하는 근거.
  - **전환 근거**: 전향적 2주 기록은 내부 도구 볼륨에서 표본이 10~20건 수준이라 검정력이 없고(문서가 F13 L3에는 검정력을 요구하면서 여기에 요구하지 않는 것은 비대칭이다), Slack PoC와 병행하면 관측 자체가 행동을 바꿔 오염되며, 2주를 대기해야 한다. 소급 분석은 세 문제를 동시에 없애고 표본을 한 자릿수 이상 키운다.
- **산출물**: PoC 리포트 / 프롬프트 v0(레지스트리 등록) / 초기 골든셋 시드 / **재질문 유형 분류표 + 필수 슬롯 초안(F2e)** / 베이스라인 수치.
- **중단·전환 기준** *(v1.4 신설, 수치는 §12)*:
  - **(a) 정제 품질 미달** — 명확화 후 문서의 개발자 검토 통과율이 기준 미만이면 Phase 1을 착수하지 않고, 프롬프트·슬롯을 재설계해 PoC를 재실행한다.
  - **(b) 마찰 과다** — 스테이크홀더 세션 완주율이 기준 미만이면 문제는 정제 품질이 아니라 **마찰**이다. Phase 1의 범위를 "질문 수 축소 + 인테이크 표면 단순화"로 재정의하고, 목업·대시보드를 더 뒤로 민다.
  - **(c) 전제의 부분적 오류** — 소급 분석에서 재질문의 다수가 **요청자 해소 불가 유형**으로 나오면 §2.1의 전제가 부분적으로 틀린 것이다. 이 경우 명확화의 무게중심을 요청자 대화(F2b 축소)에서 컨텍스트 검색·코드베이스 참조(F2a 강화)와 오픈이슈 승격(F2c)으로 옮기고, 제품 서술을 "요청자 명확화 도구"에서 "요청 라우팅·컨텍스트 보강 도구"로 재정의한다. **이 분기는 실패가 아니라 설계 정보다.**

### Phase 1 — MVP (코어 + Slack 어댑터 + 경량 웹 뷰어)
- 채널 비의존 코어: 상태 머신(§7 전체 경로), 세션·신호 스키마, 프롬프트/스키마 레지스트리, 인테이크 API, F2 전체(컨텍스트 그라운딩 + 2층 판정 + **슬롯 3상태·승격 경로**), F3(**원문 보존·슬롯 단위 확인** 포함), F5(**라우팅·SLA·이의 경로** 포함), F6(**provenance** 포함), F8(**중복 병합 + 다중 요청자 최소 모델**).
- **Slack 어댑터를 1차 표면으로**: 인테이크·스레드 명확화·게이트 버튼·역보고 DM.
- **경량 웹 뷰어**: 매직링크로 긴 문서 열람·게이트(Slack의 긴 문서 한계 보완).
- Linear 웹훅 역보고(F7 기본형 + 스펙 변경 판정 규칙).
- **롤아웃 정책 발효**: "모든 기능 요청은 PM Jude 경유" 조직 공지 + **우회 스캐너 가동**(provenance 기반 자동 집계).
- **중단·전환 기준** *(v1.4)*: 정책 발효 +4주 시점에 우회율 또는 중도 이탈율이 기준을 초과하면 **Phase 2(웹 완결 표면·목업)를 착수하지 않고**, 그 리소스를 마찰 감소(질문 수·응답 UX·첫 응답 속도)에 재투자한다. 기능을 더 얹어서 채택 실패를 해결할 수는 없다.

### Phase 2 — 웹 완결 표면 + 목업 + 평가 운영
- 웹 채팅 인테이크·대시보드(F9)·SSO·채널 전환(US-7)으로 웹을 완결 표면화(채널 비의존 원칙의 완성).
- 목업 파이프라인(F4, UI 요청 조건 실행, 호스팅 보안 전체, **역주입 포함**).
- 평가 도구 채택(F12) + 골든셋 v1 + **전수 인간 리뷰 운영(F13 L1)** — "회귀 미통과 배포 불가"를 **코드 강제로** 발효. 판독 큐 점검 항목에 스키마 결손·번역 무결성·역주입 누락 포함.

### Phase 3 — 네이티브화 + 조건부 자동화
- Linear Agents API 파일럿 → 네이티브화(에이전트 시트 비과금으로 비용 장벽 없음; 어댑터 격리 전제로 조기 파일럿 허용, 전면 전환은 GA 후 — 남은 리스크는 Developer Preview 변경뿐).
- 추가 채널(Teams/이메일).
- **F13 L2**(judge 보조·자동 분류)는 §12 임계치 충족 시에만.
- **build-vs-buy 재결정 게이트** *(v1.4 신설)*: 이 시점에 §2.5의 재결정 조건을 정식 평가한다. 트래커 벤더가 다회차 명확화 루프와 완결성 게이트를 GA로 제공하면, F2·F3의 자체 유지 여부를 재질문 유형 분류표 커버리지 기준으로 판단한다. 폐기가 정답이면 폐기한다.

### Phase 3+ (옵션)
- F13 L3(A/B·자동 개선 제안) — 통계적 검정력 확보 시에만. 내부 도구 볼륨에서 도달하지 않으면 도입하지 않는다.

---

## 10. 성공 지표
> 모든 "감소율" 지표는 **Phase 0 소급 베이스라인**을 분모로 한다. 베이스라인 없는 지표는 채택하지 않는다.
> *(v1.4)* 지표를 **선행(채택) / 후행(채택) / 품질**로 계층화한다. v1.3은 후행 지표(우회율)에 채택 판단을 전부 걸고 있었고, 그 지표가 움직였을 때는 이미 늦다.

### 선행 지표 — 채택 *(v1.4 신설, F11에서 승격)*
- **세션 중도 이탈율** — 명확화 중 응답을 멈춘 세션 비율. **우회율보다 몇 주 먼저 움직이는 채택 경보.**
- **재사용률** — 2회 이상 요청한 요청자 비율. **롤아웃 정책 강제 하에서 자발적 채택을 보여주는 사실상 유일한 신호.**
- **요청자 소요 시간** — 접수~완결까지 요청자가 실제로 소비한 시간(응답 작성 시간 합). 개발자 시간을 아끼려고 요청자 시간을 과도하게 쓰면 조직 전체로는 손해이며, 무엇보다 우회의 직접 동기가 된다.

### 후행 지표 — 채택
- **우회율** *(v1.4 — 측정 방식 교체)*: **대상 팀에서 세션 귀속(provenance) 없이 생성된 기능 요청 이슈 수 / 전체 기능 요청 이슈 수.** 버그·기술부채는 이슈 타입/라벨로 필터(F10 설정).
  - v1.3의 "개발자 1클릭 태깅" 정의는 폐기한다. **우회가 일어나는 상황(그냥 받는 게 빠름)과 성실한 태깅이 안 일어나는 상황이 정확히 같아서**, 지표가 낙관 방향으로 편향되고 그 편향은 실패가 심할수록 커진다. provenance 스캔은 개발자 액션 0으로 전수 집계된다.
  - **해석 주의**: 롤아웃 정책 발효 후 우회율은 채택 지표가 아니라 **컴플라이언스 지표**다. 강제 하의 실패 모드는 우회가 아니라 **성의 없는 통과**(한 줄 던지고 최소 답변으로 게이트 통과)이며, 이는 우회율에 잡히지 않는다. 재사용률·완결성 점수 분포·게이트 반려 사유가 이를 감지한다.

### 품질 지표
- **개발자 재질문/반려 감소율** — 소급 베이스라인 대비. 게이트 사유 태그 + 이슈 재질문·reopen 신호.
- **명확화 왕복 횟수** — 세션당 평균(완결성 점수와 함께 해석). 방향이 양의적이므로 상한 목표를 함께 둔다.
- **슬롯 승격률** *(v1.4)* — 요청자가 답할 수 없어 오픈이슈로 올라간 비율. §2.1 전제의 상시 검증치.
- **목업 승인까지 반복 횟수** — UI 요청에 한해, 목표 2~3회 이내.
- **리드타임** — 접수→이슈 생성, 접수→완료 역보고. **게이트 대기 시간을 분리 표시**(병목 진단용).
- **자기개선 루프 지표** — 버전별 품질 추이(반려율은 반드시 짝지표 — 승인 후 reopen율·후행 불일치율 — 와 함께 평가: Goodhart 방지), 골든셋 회귀 점수, 개선 사이클 리드타임, **스키마 결손 분류 건수**. judge-인간 일치율은 L2 도입 후에만.

### 목표치 *(v1.4 — Phase 0 후 확정)*
> 아래 표의 목표 열은 Phase 0 베이스라인 산출 후 채운다. **빈 칸으로 두는 것이 임의 수치를 넣는 것보다 낫다.**

| 지표 | 베이스라인 출처 | 목표 | 측정 시점 |
|---|---|---|---|
| 우회율 | 해당 없음(신규) | (Phase 0 후 확정) | 정책 발효 +4주, 이후 상시 |
| 세션 중도 이탈율 | 해당 없음(신규) | (Phase 0 PoC에서 초안) | Phase 1 상시 |
| 재사용률 | 해당 없음(신규) | (Phase 0 후 확정) | 정책 발효 +8주 |
| 요청자 소요 시간 | 해당 없음(신규) | (Phase 0 PoC에서 초안) | Phase 1 상시 |
| 개발자 재질문 건/이슈 | 소급 아카이브 | (Phase 0 후 확정) | Phase 2 |
| 이슈 reopen율 | 소급 아카이브 | (Phase 0 후 확정) | Phase 2 |
| 명확화 왕복 수 | 해당 없음(신규) | (상한 목표) | Phase 1 상시 |
| 슬롯 승격률 | 소급 분석의 해소 불가 비율 | 참조치 | Phase 1 상시 |
| 목업 반복 수 | 해당 없음(신규) | 2~3회 이내 | Phase 2 |
| 접수→이슈 리드타임 | 소급 아카이브(부분) | (Phase 0 후 확정) | Phase 1 상시 |

---

## 11. 리스크와 완화책
- **채택 실패(최대 리스크)** — 스테이크홀더가 봇을 우회하고 개발자에게 직접 요청. 완화: Slack 우선 전달(서식지), 첫 응답 3초, 롤아웃 정책 공지, **provenance 기반 우회 자동 집계**, **선행 지표(이탈율·재사용률·요청자 소요 시간) 상시 관측**, Phase 1 중단 기준(§9) 발동 시 기능 추가 대신 마찰 감소에 재투자.
- **게이트 병목** *(v1.4 신설)* — 모든 요청이 개발자 1인의 결정을 통과하므로 제품이 **새 병목을 만든다**. 완화: 라우팅 규칙·SLA·미응답 자동 백로그(F5), 게이트 대기 시간을 리드타임에서 분리해 상시 관측, 대기 시간 악화 시 승인자 다중화 또는 저위험 요청 유형의 자동 승인 규칙 검토.
- **완결성 스키마 오정렬** *(v1.4 신설)* — 슬롯을 다 채운 문서가 여전히 재질문을 유발하면 개선 루프가 프롬프트만 만지며 헛돈다. 완화: F2e(슬롯 근거 관리), 재질문 유형↔슬롯 매핑을 분기 점검, 미매핑 유형은 **스키마 결손**으로 별도 분류(F13).
- **번역 무결성** *(v1.4 신설)* — 요구사항 정밀성이 제품의 존재 이유인데 번역 구간만 검증 공백이었다. 완화: 원문 상시 보존, 슬롯 단위·요청자 언어 확인(원칙 7), 판독 큐 표본 대조.
- **요청자 해소 불가 비율이 높을 가능성** *(v1.4 신설)* — §2.1의 전제가 부분적으로 틀릴 수 있다. 완화: Phase 0 소급 분석으로 사전 실측, 슬롯 승격 경로로 런타임 대응, 비율이 높으면 §9 Phase 0 (c) 분기로 설계 재조정.
- **Linear Agents API preview 의존** — 핵심 경로는 `issueCreate`+웹훅만 사용, 네이티브화는 어댑터 뒤 격리. 비용 리스크 없음(에이전트 시트 비과금). 관리 대상은 GA 전 변경 가능성. **추가 관측** *(v1.4)*: 벤더가 고볼륨 자동화 기능의 과금 모델을 GA 시점에 변경할 수 있다고 예고했으므로, 네이티브화 결정 시 비용 가정을 재확인한다.
- **LLM 모호성 판정 한계** — 2층 판정(F2c): 룰 백스톱이 과신 통과를 차단, 임계치는 F12 회귀로 튜닝.
- **컨텍스트 검색 품질** — 그라운딩 검색이 엉뚱한 문서를 참조하면 질문이 오염된다. 완화: 검색 결과를 세션에 기록해 판독 시 검증, 호출 상한, 참조 문서 화이트리스트. **v1.4 추가 위험**: 검색 대상에 과거 거절 이력이 포함되면서, 오래되어 무효가 된 거절을 근거로 새 요청을 잘못 차단할 수 있다. 완화: 결정 이력에 유효기간 개념 부여, 거절 이력은 **차단이 아니라 참고 정보로만** 제시.
- **목업 경계의 정직성** — 목업 생성은 코드 생성이며 경계는 정책 규율임을 문서가 인정(원칙 3). 완화: 코드 비전달·라벨 상시·판독 시 위반 점검.
- **목업-문서 진실 원천 분기** *(v1.4 신설)* — 어노테이션에서 확정된 사항이 목업에만 남으면 문서가 진실 원천이 아니게 된다. 완화: 역주입 단계(F4), 게이트의 흡수 완료 표시, 판독 큐 점검.
- **목업 기대치 오해 / 호스팅 보안** — 워터마크·문구 반복, §6 다층 방어.
- **거절의 조직적 파장** *(v1.4 신설)* — 개발자가 서열이 높은 요청자를 거절하는 상황. 완화: 통제된 사유 목록, 이의 경로 명시(F5/F8), 반복 이의는 팀 리드가 정책으로 흡수.
- **자기개선 과설계** — 저볼륨에서 judge/A-B는 무의미. 완화: L1 전수 인간 리뷰 기본, L2/L3 임계치 조건부. **v1.4 확장**: 동일 렌즈를 비용 가드레일·모델 라우팅에도 적용(§6).
- **LLM-judge 편향(도입 시)** — judge·생성 모델 분리, 인간 판독 라벨로 보정, 일치율 관리.
- **Goodhart** — 반려율 단독 최적화 금지, 짝지표 동반 평가. **우회율도 정책 강제 후에는 컴플라이언스 지표임을 명시**(§10).
- **골든셋 오염·노후화 / 개선 드리프트 / 평가 프라이버시** — 평가·개선용 분리, 분기 갱신, 승인 게이트·카나리·롤백 유지, 익명화(§6).
- **다중 스테이크홀더 상충** — 중복 병합이 동작하는 순간 1이슈 N요청자가 발생한다. 완화(Phase 1 최소 모델): 요청자 목록 보유, 역보고 팬아웃, 중복 후보 매칭으로 상충 세션 표면화, 최종 판단은 게이트의 개발자. 정식 모델은 §12.
- **벤더 기능 확장** *(v1.4 재프레이밍)* — "해자 축소" 프레임을 폐기하고 **build-vs-buy 재결정 트리거**로 관리한다(§2.5). 내부 도구는 대체되기를 기꺼워해야 하며, 어댑터 격리와 얇은 추상화가 그 재결정 비용을 낮춘다. 분기별 재평가 + Phase 3의 정식 게이트.

---

## 12. 오픈 이슈 / 결정 대기 항목

### v1.4에서 닫힌 항목
- ~~완결성 룰 층의 필수 슬롯 최종 목록~~ → **절차로 확정**: Phase 0 소급 분석의 재질문 유형 분류표에서 도출(F2e). 목록 자체는 데이터가 정한다.
- ~~팀 표준 문서 언어와 원문 병기 정책~~ → **확정**: 문서는 팀 표준 언어, **원문 전사는 옵션이 아니라 상시 보존**, 요청자 확인은 슬롯 단위·요청자 언어(원칙 7).
- ~~개발자 "우회" 태깅 UX~~ → **불필요**: provenance 기반 자동 집계로 대체(§10). "스펙 불충분" 태깅 UX만 남는다.
- ~~프로바이더 추상화 구현체 벤치마크~~ → **불필요**: 볼륨상 비용이 제약이 아니므로 벤치마크 없이 하나를 지금 확정(§6).

### 남은 결정 대기 항목
1. 완결성 룰 층의 **금칙 모호어 사전** — PoC에서 초안, F12 회귀로 확정.
2. LLM 층 루브릭 임계치 — F12 회귀로 튜닝.
3. **명확화 왕복 상한 수치** *(v1.4 추가 — 목업 상한과 대칭)*와 상한 도달 시 승격 판정의 기준.
4. **슬롯 `승격` 판정 기준** *(v1.4 신설)* — 무엇을 "요청자가 원리적으로 답할 수 없음"으로 볼 것인가. 남용되면 정제가 무력화되고, 인색하면 데드락이 돌아온다. PoC 세션으로 초안.
5. **게이트 SLA 수치와 근무시간 정의** *(v1.4 신설)*, SLA 초과 자동 백로그의 유예 단계 수.
6. **거절 이의 경로의 운영 주체와 절차** *(v1.4 신설)* — 팀 리드 단독 판단 vs. 주간 리뷰 안건.
7. 목업 반복 상한(2 vs. 3)과 초과 시 에스컬레이션 경로.
8. 세션당 토큰·목업 폭주 방지 상한 수치 + 평가 예산.
9. SSO(SAML vs. OIDC)와 매직 링크 병행 정책 / 목업 서명 URL 만료 기본값.
10. **Phase 0 중단·전환 기준의 수치화** *(v1.4 신설)* — 개발자 검토 통과율 X%, 세션 완주율 Y%, "요청자 해소 불가" 비율의 재설계 임계치 Z%.
11. **Phase 1 중단 기준 수치** *(v1.4 신설)* — 정책 발효 +4주 시점의 우회율·이탈율 상한.
12. **우회 집계의 "기능 요청 이슈" 필터 정의** *(v1.4 신설)* — 어떤 이슈 타입/라벨을 분모에 넣을 것인가. 잘못 잡으면 지표가 통째로 무의미해진다.
13. F13 L2 도입 임계치 수치(주간 세션 수 N, 축적 라벨 수 M, judge-인간 일치율 기준) — L1 운영 데이터로 확정.
14. 백로그 재부상 트리거 수치(유사 요청 누적 N회, 정기 리뷰 주기).
15. **다중 스테이크홀더 상충 해소의 정식 모델** — Phase 1 최소 모델 운영 후 확정.
16. **과거 거절 이력의 유효기간** *(v1.4 신설)* — 언제까지 참고 정보로 제시할 것인가.
17. PoC Exit 기준 / `claude -p` vs Agent SDK 선택, 인증 방식, 실행 환경 — 보안 검토 후.
18. Agents API 조기 파일럿 착수 조건(Preview 변경 모니터링 체계).

---

## 13. 용어집 (비개발자 독자용)
- **인테이크(Intake)** — 들어온 요청을 받아 방어·정제하는 첫 단계.
- **명확화 루프** — 질문-답변 반복으로 모호성을 해소하는 대화 과정. 수행 주체는 LLM.
- **컨텍스트 그라운딩** — 질문을 만들기 전에 기존 이슈·문서·과거 결정을 검색해 요청을 맥락 위에 놓는 것. 이미 답이 있는 것을 묻지 않기 위한 장치.
- **완결성 게이트 / 2층 판정** — 정제 완료 여부를 (1) 룰 층: 필수 슬롯·형식의 기계적 검사(결정론적), (2) LLM 층: 남은 의미적 모호성 판정의 두 겹으로 결정하는 구조.
- **슬롯 3상태 / 승격** *(v1.4)* — 각 필수 항목은 `충족`·`미충족`·`승격` 중 하나다. **승격**은 "요청자가 원리적으로 답할 수 없어 담당자 지정 오픈이슈로 올린 항목"을 뜻한다. 승격이 있으면 요청은 멈추지 않고 "조건부"로 개발자에게 간다.
- **중복 병합** *(v1.4)* — 같은 요청이 이미 있을 때 새 이슈를 만들지 않고 기존 이슈에 요청자를 붙이는 종결 방식. 요청자는 그대로 진행 상황을 받는다.
- **역주입** *(v1.4)* — 목업 위 코멘트에서 확정된 내용을 요구사항 문서 문장으로 되돌려 넣는 것. 문서만 남겨도 구현할 수 있게 만들기 위함.
- **세션 귀속(provenance)** *(v1.4)* — 만들어진 이슈에 "어느 세션에서 나왔는지"를 기계가 읽을 수 있게 남기는 것. 이것이 있어야 우회를 자동으로 셀 수 있다.
- **문서 단일 진실 원천** *(v1.4)* — 구현의 근거는 요구사항 문서 하나뿐이며, 대화·목업·번역본은 그것을 대체하지 않는다는 원칙.
- **고정 오케스트레이션(사전 정의된 제어 흐름)** — 단계의 순서·전이를 LLM이 아니라 코드의 상태 머신이 결정하는 방식. 자율 에이전트 루프의 반대 개념.
- **결정론적** *(용법 한정)* — 같은 입력이면 항상 같은 출력이 나오고 무작위성이 없다는 뜻. LLM 호출은 결정론적이지 않다. 본 문서에서는 룰 기반 검사와 게이트 강제 코드에만 이 표현을 쓴다.
- **선행 지표 / 후행 지표** *(v1.4)* — 선행 지표는 문제가 커지기 전에 먼저 움직이는 수치(세션 이탈율), 후행 지표는 결과가 확정된 뒤에 보이는 수치(우회율). 둘 다 필요하되, 판단은 선행 지표로 한다.
- **베이스라인** — 도입 전에 측정해 두는 기준 수치. "감소율" 지표의 분모. v1.4에서는 기존 이슈 아카이브의 **소급 분석**으로 만든다.
- **EARS** — 정형 요구사항 구문(Mavin et al., IEEE RE'09). **INVEST** — 좋은 유저스토리 6기준(Bill Wake, 2003). **Given-When-Then** — BDD 수용기준 서식.
- **코딩 에이전트** — 개발자가 코드를 짜는 데 쓰는 AI 도구. 본 제품은 스펙만 넘긴다.
- **채널 어댑터** — 코어를 특정 표면(웹/Slack/Teams…)에 연결하는 탈부착 모듈.
- **언퍼링** — 메신저의 링크 미리보기 자동 생성. 데이터 유출 통로가 될 수 있어 끈다.
- **목업** — 요구 확인용 중간충실도 인터랙티브 화면. 구현 결과물·코드 기준이 아니다.
- **우회율** — PM Jude를 거치지 않고 개발자에게 직접 간 요청의 비율. 정책 강제 후에는 채택이 아니라 **컴플라이언스** 지표로 읽는다.
- **LLM-as-judge / 골든 데이터셋 / 회귀 평가 / 프롬프트 레지스트리 / 카나리 배포 / Goodhart 법칙** — v1.1 정의 유지(judge는 L2 도입 후 활성).
- **헤드리스 하네스(`claude -p` / Claude Agent SDK)** — Claude Code를 UI 없이 프로그램적으로 실행하는 방식. Phase 0 한정 임시 백엔드.
- **PoC** — 최소 구성으로 핵심 가설(명확화 품질·채택)을 검증하는 선행 단계.

---

## 부록 A — 참고 링크
- ChatPRD: chatprd.ai · Linear Asks/Intake: linear.app/asks, linear.app/intake
- Linear Agent 공개 베타(2026-03): linear.app/changelog/2026-03-24-introducing-linear-agent · Linear for Agents(Developer Preview): linear.app/developers/agents, /agent-interaction · 에이전트 시트 비과금: linear.app/docs/agents-in-linear
- Linear Webhooks: linear.app/developers/webhooks
- ClarifyGPT: arXiv:2310.10996 (FSE/ACM 2024)
- EARS: alistairmavin.com/ears · INVEST: xp123.com (Bill Wake, 2003)
- Slack Bolt Assistant: docs.slack.dev/tools/bolt-js · 언퍼링 보안: docs.slack.dev/messaging/unfurling-links-in-messages
- Claude Artifacts 샌드박스 패턴: code.claude.com/docs/en/artifacts · CSP sandbox: content-security-policy.com/sandbox
- Cloudflare R2/Workers: developers.cloudflare.com
- 모델 추상화: ai-sdk.dev · docs.litellm.ai · AWS Bedrock Converse API
- 오케스트레이션 원칙(workflows vs agents, "predefined code paths"): anthropic.com/research/building-effective-agents
- 평가 도구(채택 후보): langfuse.com(셀프호스트) · promptfoo.dev · braintrust.dev
- Claude Code 헤드리스/Agent SDK: code.claude.com/docs

*비고: 외부 도구·API 상태는 2026-07 기준이며 변동 가능. Linear for Agents API는 Developer Preview로 GA 전 변경 가능성이 명시되어 있고, Linear Agent는 2026-03 공개 베타 상태다. 벤더가 고볼륨 자동화 기능의 과금 모델을 GA 시점에 변경할 수 있다고 예고한 점은 Phase 3 네이티브화 판단 시 재확인 대상이다.*
